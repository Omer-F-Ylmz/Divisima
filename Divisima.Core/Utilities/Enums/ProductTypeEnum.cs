namespace Divisima.Core.Utilities.Enums
{
    // Açıklayıcı yorum: Ürün tipi. Product.product_type (byte). Beden-stok mantığı buna göre.
    public enum ProductTypeEnum
    {
        Clothing = 0,     // Giysi (bedenli)
        Accessory = 1     // Aksesuar (bedensiz)
    }
}
